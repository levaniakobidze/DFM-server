const REPORT_TARGET_TYPES = ['DARE', 'SUBMISSION'];
const REPORT_STATUSES = ['OPEN', 'REVIEWED', 'DISMISSED'];

// Valid status transitions for a Report.
const REPORT_STATUS_TRANSITIONS = {
  OPEN: ['REVIEWED', 'DISMISSED'],
  REVIEWED: ['DISMISSED'],
  DISMISSED: [],
};

const REPORT_DEFAULT_PAGE_SIZE = 20;
const REPORT_MAX_PAGE_SIZE = 50;

module.exports = {
  REPORT_TARGET_TYPES,
  REPORT_STATUSES,
  REPORT_STATUS_TRANSITIONS,
  REPORT_DEFAULT_PAGE_SIZE,
  REPORT_MAX_PAGE_SIZE,
};
